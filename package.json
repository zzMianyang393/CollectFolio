{
  "name": "collectfolio",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "CollectFolio — cross-category collectibles portfolio manager (landing page, demand smoke test phase)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "wrangler pages deploy ./dist",
    "check": "astro check",
    "verify:seo": "node scripts/verify-seo.mjs",
    "test:waitlist": "node --test --experimental-strip-types functions/api/waitlist-logic.test.ts",
    "test": "npm run test:waitlist && npm run build && npm run verify:seo"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/rss": "^4.0.9",
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource-variable/inter": "^5.1.0",
    "@fontsource/ibm-plex-mono": "^5.1.0",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.0.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "vite": "^6.4.3",
    "wrangler": "^4.104.0"
  }
}
